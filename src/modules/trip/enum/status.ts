export enum Status {
  Draft = 'draft',
  Active = 'active',
  Happening = 'happening',
  Done = 'done',
  Cancel = 'cancel',
}

export enum TripMemberStatus {
  Invited = 'invited',
  Requested = 'requested',
  Approved = 'approved',
  RejectRequest = 'reject_request',
  RejectInvite = 'reject_invite',
}
